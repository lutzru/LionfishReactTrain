schema: ../graphql/schema.graphqls
generates:
    ./src/graphql/types.ts:
        plugins:
            - typescript
        config:
            namingConvention:
                enumValues: upper-case#upperCase
